### `__EXP__ 2 - Conditional serving`

Motivation:

Serve your app based on a condition located in fragment.

Build image:

`docker build -t exp2:latest -f Dockerfile .`

Interactively run the container:

`docker run -p 8080:80 exp2:latest`
